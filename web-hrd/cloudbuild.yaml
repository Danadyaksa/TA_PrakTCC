steps:
  # Step 1: Install dependencies
  - name: "node:22"
    entrypoint: npm
    args: ["ci"]
    dir: "web-hrd"

  # Step 2: Build Next.js
  - name: "node:22"
    entrypoint: npm
    args: ["run", "build"]
    dir: "web-hrd"
    env:
      - "NEXT_PUBLIC_API_URL=${_BACKEND_URL}/api"
      - "NODE_ENV=production"

  # Step 3: Inject backend URL ke app.yaml
  - name: "bash"
    args:
      - "-c"
      - |
        sed -i "s|REPLACE_WITH_BACKEND_URL|${_BACKEND_URL}|g" web-hrd/app.yaml

  # Step 4: Deploy ke App Engine
  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    entrypoint: gcloud
    args:
      - app
      - deploy
      - web-hrd/app.yaml
      - --quiet
      - --project=$PROJECT_ID

options:
  logging: CLOUD_LOGGING_ONLY

substitutions:
  _BACKEND_URL: "https://presensi-backend-820401822458.asia-southeast2.run.app"
